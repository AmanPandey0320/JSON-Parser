package json.parsor.builder;

import json.parsor.tokeniser.Token;

import java.util.ArrayList;
import java.util.HashMap;

public class StructureProcessor {

    public ArrayList<?> destructureArray(String key, HashMap<String, Token> keyToTokenMap, Class<?> clazz){
        ArrayList<?> arr = new ArrayList<>();

        // check for nested object in array


        return arr;
    }
}
